diff --git a/composables/useApi.ts b/composables/useApi.ts
index 11e1236..eb11b2e 100644
--- a/composables/useApi.ts
+++ b/composables/useApi.ts
@@ -214,10 +214,6 @@ export function useRequest() {

   const headers = new Headers()

-  if (endpoint.secret) {
-    headers.set('Authorization', `Bearer ${endpoint.secret}`)
-  }
-
   return ky.create({
     prefix: endpoint.url,
     headers,
@@ -227,7 +223,6 @@ export function useRequest() {
         ({ response }) => {
           if (!isUnauthorizedStatus(response.status)) return
           if (endpoint.id === 'proxy-runtime-mihomo') {
-            endpointStore.setSelectedEndpoint('')
             redirectToProxyRuntimeSetup(endpoint.id)
           }
         },
@@ -257,11 +252,11 @@ export type EndpointCheckError =
   | null
 export function checkEndpointAPI(
   url: string,
-  secret: string,
+  _secret: string,
 ): Promise<EndpointCheckError> {
   return ky
     .get(url.endsWith('/') ? `${url}version` : `${url}/version`, {
-      headers: secret ? { Authorization: `Bearer ${secret}` } : {},
+      credentials: 'same-origin',
       timeout: 5000,
     })
     .then(() => null)
diff --git a/composables/useWebSocket.ts b/composables/useWebSocket.ts
index ed6c0d6..3264c28 100644
--- a/composables/useWebSocket.ts
+++ b/composables/useWebSocket.ts
@@ -2,10 +2,6 @@ import type { Log, MemoryData, TrafficData, WsMsg } from '~/types'
 import { getCurrentScope, onScopeDispose } from 'vue'
 import { useMockMode } from './useApi'
 import { useMockData } from './useMockData'
-import {
-  proxyRuntimeEndpointNeedsSetup,
-  redirectToProxyRuntimeSetup,
-} from '~/composables/proxyRuntimeEndpointAuth'
 // Delay before retrying after an unexpected socket close (e.g. "Restart Core").
 const RECONNECT_DELAY = 3000

@@ -56,17 +52,8 @@ export function useBackendWebSocket() {
   ) => {
     const endpoint = endpointStore.currentEndpoint
     if (!endpoint) return null
-    if (proxyRuntimeEndpointNeedsSetup(endpoint)) {
-      redirectToProxyRuntimeSetup(endpoint.id)
-      return null
-    }
     const wsUrl = endpointStore.wsEndpointURL
-    const secret = endpoint.secret
-
-    const params = new URLSearchParams()
-    if (secret) params.set('token', secret)
-
-    const ws = new WebSocket(`${wsUrl}/${path}?${params.toString()}`)
+    const ws = new WebSocket(`${wsUrl}/${path}`)

     ws.onmessage = (event) => {
       try {
@@ -236,14 +223,8 @@ export function useBackendWebSocket() {
   const createLogsWebSocket = (): WebSocket | null => {
     const endpoint = endpointStore.currentEndpoint
     if (!endpoint) return null
-    if (proxyRuntimeEndpointNeedsSetup(endpoint)) {
-      redirectToProxyRuntimeSetup(endpoint.id)
-      return null
-    }
     const wsUrl = endpointStore.wsEndpointURL
-    const params = new URLSearchParams()
-    if (endpoint.secret) params.set('token', endpoint.secret)
-    params.set('level', configStore.logLevel)
+    const params = new URLSearchParams({ level: configStore.logLevel })

     const ws = new WebSocket(`${wsUrl}/logs?${params.toString()}`)
     ws.onmessage = (event) => {
diff --git a/pages/setup.vue b/pages/setup.vue
index 834d2e9..d5106c2 100644
--- a/pages/setup.vue
+++ b/pages/setup.vue
@@ -3,7 +3,6 @@ import type { EndpointCheckError } from '~/composables/useApi'
 import { IconLink, IconLock, IconServer } from '@tabler/icons-vue'
 import { checkEndpointAPI } from '~/composables/useApi'
 import {
-  proxyRuntimeAuthRequired,
   proxyRuntimeMihomoEndpointID,
 } from '~/composables/proxyRuntimeEndpointAuth'

@@ -63,7 +62,7 @@ async function onSubmit() {

   try {
     const url = fixedEndpointURL()
-    const secret = formData.secret
+    const secret = ''

     formData.url = url
     const error = await checkEndpointAPI(url, secret)
@@ -82,9 +81,8 @@ async function onSubmit() {

 onMounted(async () => {
   syncFormEndpoint()
-  if (!proxyRuntimeAuthRequired() || formData.secret) {
-    await onSubmit()
-  }
+  persistFixedEndpoint('')
+  onSetupSuccess()
 })
 </script>

diff --git a/stores/endpoint.ts b/stores/endpoint.ts
index e98235e..e558c10 100644
--- a/stores/endpoint.ts
+++ b/stores/endpoint.ts
@@ -1,15 +1,36 @@
 import type { Endpoint } from '~/types'
 import { defineStore } from 'pinia'

+const proxyRuntimeEndpointID = 'proxy-runtime-mihomo'
+
+function fixedProxyRuntimeEndpoint(): Endpoint {
+  const url =
+    typeof window === 'undefined'
+      ? '/mihomo/controller'
+      : new URL('/mihomo/controller', window.location.origin).href.replace(
+          /\/$/,
+          '',
+        )
+  return { id: proxyRuntimeEndpointID, url, secret: '' }
+}
+
 export const useEndpointStore = defineStore('endpoint', () => {
   // State
-  const selectedEndpoint = useLocalStorage<string>('selectedEndpoint', '')
+  const selectedEndpoint = useLocalStorage<string>(
+    'selectedEndpoint',
+    proxyRuntimeEndpointID,
+  )
   const endpointList = useLocalStorage<Endpoint[]>('endpointList', [])

+  function syncFixedEndpoint() {
+    selectedEndpoint.value = proxyRuntimeEndpointID
+    endpointList.value = [fixedProxyRuntimeEndpoint()]
+  }
+
+  if (typeof window !== 'undefined') syncFixedEndpoint()
+
   // Getters
-  const currentEndpoint = computed(() =>
-    endpointList.value.find(({ id }) => id === selectedEndpoint.value),
-  )
+  const currentEndpoint = computed(() => fixedProxyRuntimeEndpoint())

   const wsEndpointURL = computed(() => {
     const endpoint = currentEndpoint.value
@@ -24,31 +45,24 @@ export const useEndpointStore = defineStore('endpoint', () => {
   })

   // Actions
-  const setSelectedEndpoint = (id: string) => {
-    selectedEndpoint.value = id
+  const setSelectedEndpoint = (_id: string) => {
+    syncFixedEndpoint()
   }

-  const setEndpointList = (list: Endpoint[]) => {
-    endpointList.value = list
+  const setEndpointList = (_list: Endpoint[]) => {
+    syncFixedEndpoint()
   }

-  const addEndpoint = (endpoint: Endpoint) => {
-    endpointList.value = [endpoint, ...endpointList.value]
+  const addEndpoint = (_endpoint: Endpoint) => {
+    syncFixedEndpoint()
   }

-  const removeEndpoint = (id: string) => {
-    endpointList.value = endpointList.value.filter((e) => e.id !== id)
-    if (selectedEndpoint.value === id) {
-      selectedEndpoint.value = ''
-    }
+  const removeEndpoint = (_id: string) => {
+    syncFixedEndpoint()
   }

-  const updateEndpoint = (id: string, updates: Partial<Endpoint>) => {
-    const index = endpointList.value.findIndex((e) => e.id === id)
-    const existing = endpointList.value[index]
-    if (index !== -1 && existing) {
-      endpointList.value[index] = { ...existing, ...updates } as Endpoint
-    }
+  const updateEndpoint = (_id: string, _updates: Partial<Endpoint>) => {
+    syncFixedEndpoint()
   }

   return {
